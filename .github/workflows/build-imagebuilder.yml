name: 1. Build ImageBuilder
run-name: "Build ImageBuilder — ${{ inputs.force_commit && format('commit {0} ({1})', inputs.force_commit, inputs.force_branch_name) || inputs.force_branch || 'auto-discover' }} on ${{ inputs.runner || 'ubuntu-latest' }}"

on:
  schedule:
    - cron: "0 4 * * 0" # Weekly Sunday 04:00 UTC
  workflow_dispatch:
    inputs:
      force_branch:
        description: "Mode 2: force a specific branch from MT6000_cust_build (e.g. next-r4.8.0.rss.mtk-test6.18)"
        required: false
        type: string
      force_commit:
        description: "Mode 3: full SHA on pesa1234/openwrt — bypasses MT6000_cust_build entirely"
        required: false
        type: string
      force_branch_name:
        description: "Mode 3: branch name to use with force_commit (e.g. next-r4.8.1.rss.mtk)"
        required: false
        type: string
      force_config_url:
        description: "Mode 3: optional config.buildinfo URL override (auto-falls back to latest same-X.Y if blank)"
        required: false
        type: string
      runner:
        description: "Runner to use for the build job"
        required: true
        type: choice
        default: ubuntu-latest
        options:
          - ubuntu-latest
          - build-unraid
          - build-smospab

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ogglord/openwrt-imagebuilder

jobs:
  discover:
    runs-on: ubuntu-latest
    outputs:
      branch: ${{ steps.discover.outputs.branch }}
      asu_version: ${{ steps.discover.outputs.asu_version }}
      commit_sha: ${{ steps.discover.outputs.commit_sha }}
      rev_hash: ${{ steps.discover.outputs.rev_hash }}
      release_dir: ${{ steps.discover.outputs.release_dir }}
      new_commits: ${{ steps.discover.outputs.new_commits }}
    steps:
      - name: Discover latest release dir and check for new commits
        id: discover
        env:
          FORCE_BRANCH: ${{ inputs.force_branch }}
          FORCE_COMMIT: ${{ inputs.force_commit }}
          FORCE_BRANCH_NAME: ${{ inputs.force_branch_name }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          # Strip next-r prefix, take first 3 version components.
          derive_asu_version() {
            echo "$1" | sed 's/^next-r//' | cut -d. -f1,2,3
          }

          # ── Mode 3: force_commit — bypass MT6000_cust_build entirely ──────────
          if [[ -n "${FORCE_COMMIT:-}" ]]; then
            if [[ -z "${FORCE_BRANCH_NAME:-}" ]]; then
              echo "::error::force_commit requires force_branch_name to be set"
              exit 1
            fi
            BRANCH="$FORCE_BRANCH_NAME"
            LATEST_SHA="$FORCE_COMMIT"
            ASU_VER=$(derive_asu_version "$BRANCH")

            # rREV-HASH via compare API — avoids shallow-clone trap; REBOOT is getver.sh's count origin.
            REBOOT_SHA="ee53a240ac902dc83209008a2671e7fdcf55957a"
            AHEAD=$(gh api "repos/pesa1234/openwrt/compare/${REBOOT_SHA}...${LATEST_SHA}" --jq .ahead_by)
            SHORT_HASH="${LATEST_SHA:0:10}"
            REV_HASH="r${AHEAD}-${SHORT_HASH}"

            # Resolve fallback release_dir for config/manifest steps.
            XY=$(echo "$BRANCH" | sed 's/^next-r//' | cut -d. -f1,2)
            RELEASE_DIR=$(curl -fsSL "https://api.github.com/repos/pesa1234/MT6000_cust_build/contents/" \
              -H "Accept: application/vnd.github.v3+json" | \
              jq -r '.[] | select(.type == "dir") | .name' | \
              grep -E '^[0-9]{4}' | grep "next-r${XY}\." | sort | tail -1 || true)
            if [[ -z "$RELEASE_DIR" ]]; then
              echo "::warning::No fallback release dir for X.Y=${XY} in MT6000_cust_build — config/manifest steps must use force_config_url"
              RELEASE_DIR=""
            else
              echo "Mode 3 fallback release dir: ${RELEASE_DIR}"
            fi

            echo "Mode:        force_commit"
            echo "Branch:      ${BRANCH}"
            echo "Commit SHA:  ${LATEST_SHA}"
            echo "ASU version: ${ASU_VER}"
            echo "Rev hash:    ${REV_HASH}"

            # Mode 3 skips ALREADY_BUILT — caller explicitly chose the commit.
            echo "new_commits=true"     >> "$GITHUB_OUTPUT"
            echo "branch=${BRANCH}"     >> "$GITHUB_OUTPUT"
            echo "asu_version=${ASU_VER}" >> "$GITHUB_OUTPUT"
            echo "commit_sha=${LATEST_SHA}" >> "$GITHUB_OUTPUT"
            echo "rev_hash=${REV_HASH}" >> "$GITHUB_OUTPUT"
            echo "release_dir=${RELEASE_DIR}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # ── Modes 1 & 2: discover from MT6000_cust_build ─────────────────────
          DIRS=$(curl -fsSL "https://api.github.com/repos/pesa1234/MT6000_cust_build/contents/" \
            -H "Accept: application/vnd.github.v3+json" | \
            jq -r '.[] | select(.type == "dir") | .name' | \
            grep -E '^[0-9]{4}' || true)

          if [[ -z "$DIRS" ]]; then
            echo "::error::No release directories found in MT6000_cust_build"
            echo "new_commits=false" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          # Mode 2: force_branch — pick latest dir for that branch
          # Mode 1: auto — pick absolute latest dir (test branches included)
          if [[ -n "${FORCE_BRANCH:-}" ]]; then
            RELEASE_DIR=$(echo "$DIRS" | grep -E "_${FORCE_BRANCH}$" | sort | tail -1 || true)
            if [[ -z "$RELEASE_DIR" ]]; then
              echo "::error::No release dir for branch '${FORCE_BRANCH}' in MT6000_cust_build"
              exit 1
            fi
            echo "Mode: force_branch (${FORCE_BRANCH})"
          else
            RELEASE_DIR=$(echo "$DIRS" | sort | tail -1)
            echo "Mode: auto-discover"
          fi

          # Parse: YYYY-MM-DD_rREV-HASH_BRANCH  (hash is short, branch may contain hyphens)
          BRANCH=$(echo "$RELEASE_DIR" | sed -E 's/^[0-9]{4}-[0-9]{2}-[0-9]{2}_r[0-9]+-[a-f0-9]+_//')
          RELEASE_COMMIT=$(echo "$RELEASE_DIR" | sed -E 's/^[0-9]{4}-[0-9]{2}-[0-9]{2}_r[0-9]+-([a-f0-9]+)_.*/\1/')
          if [[ -z "$BRANCH" || -z "$RELEASE_COMMIT" || "$BRANCH" == "$RELEASE_DIR" ]]; then
            echo "::error::Could not parse release dir: $RELEASE_DIR"
            exit 1
          fi

          echo "Release dir:    ${RELEASE_DIR}"
          echo "Branch:         ${BRANCH}"
          echo "Release commit: ${RELEASE_COMMIT} (short)"

          # Resolve short hash → full SHA
          LATEST_SHA=$(gh api "repos/pesa1234/openwrt/commits/${RELEASE_COMMIT}" --jq .sha)
          echo "Full commit SHA: ${LATEST_SHA}"

          # Skip if we've already built this commit for this branch (auto only)
          TAG="mediatek-filogic-${BRANCH}"
          ALREADY_BUILT=$(gh api "repos/${{ github.repository }}/releases/tags/${TAG}" --jq .body 2>/dev/null | grep -c "${LATEST_SHA}" || true)
          if [[ "$ALREADY_BUILT" -gt 0 && -z "${FORCE_BRANCH:-}" ]]; then
            echo "Commit ${LATEST_SHA} already built — skipping"
            echo "new_commits=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          ASU_VER=$(derive_asu_version "$BRANCH")
          REV_HASH=$(echo "$RELEASE_DIR" | grep -oE 'r[0-9]+-[a-f0-9]+' | head -1)

          echo "new_commits=true"       >> "$GITHUB_OUTPUT"
          echo "branch=${BRANCH}"       >> "$GITHUB_OUTPUT"
          echo "asu_version=${ASU_VER}" >> "$GITHUB_OUTPUT"
          echo "commit_sha=${LATEST_SHA}" >> "$GITHUB_OUTPUT"
          echo "rev_hash=${REV_HASH}"   >> "$GITHUB_OUTPUT"
          echo "release_dir=${RELEASE_DIR}" >> "$GITHUB_OUTPUT"


  build:
    name: build (${{ needs.discover.outputs.branch }})
    needs: discover
    if: needs.discover.outputs.new_commits == 'true'
    runs-on: ${{ inputs.runner || 'ubuntu-latest' }}
    timeout-minutes: 360

    env:
      IS_GH_RUNNER: ${{ inputs.runner == 'ubuntu-latest' || inputs.runner == '' }} # true when GitHub-hosted
      FORCE_UNSAFE_CONFIGURE: 1
      LC_ALL: C
      TAR_OPTIONS: --no-same-owner

    steps:
      - name: Healthcheck — start
        run: curl -fsS -m 10 --retry 3 "https://hc-ping.com/${{ secrets.HEALTHCHECK_BUILD_UUID }}/start" || true # tolerate failure

      - uses: actions/checkout@v5

      - name: Free disk space (GitHub-hosted)
        if: env.IS_GH_RUNNER == 'true'
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
          swap-storage: true

      - name: Install build dependencies (GitHub-hosted)
        if: env.IS_GH_RUNNER == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang flex bison g++ gawk gcc-multilib \
            g++-multilib gettext git libncurses-dev libssl-dev \
            python3-setuptools rsync swig unzip \
            zlib1g-dev file wget curl zstd
          sudo apt-get autoremove -y && sudo apt-get clean -y

      - name: Clean self-hosted workspace
        if: env.IS_GH_RUNNER == 'false'
        run: rm -rf ${{ runner.temp }}/openwrt

      - name: Clone OpenWrt source at release commit
        env:
          BRANCH: ${{ needs.discover.outputs.branch }}
          RELEASE_SHA: ${{ needs.discover.outputs.commit_sha }}
        run: |
          set -euo pipefail
          # Depth-1 fetch of exact commit for byte-parity with pesa's release.
          DEST="${{ runner.temp }}/openwrt"
          mkdir -p "$DEST" && cd "$DEST"
          git init --quiet
          git remote add origin https://github.com/pesa1234/openwrt.git
          git fetch --depth 1 origin "$RELEASE_SHA"
          git checkout --quiet FETCH_HEAD
          echo "Checked out $(git rev-parse HEAD) on ${BRANCH}"

      - name: Link dl/ to persistent cache (self-hosted)
        if: env.IS_GH_RUNNER == 'false'
        run: |
          mkdir -p "$HOME/.cache/openwrt-dl"
          ln -sf "$HOME/.cache/openwrt-dl" "${{ runner.temp }}/openwrt/dl"
          echo "Linked dl/ → $HOME/.cache/openwrt-dl ($(ls "$HOME/.cache/openwrt-dl" | wc -l) cached files)"

      - name: Cache dl/ (GitHub-hosted)
        if: env.IS_GH_RUNNER == 'true'
        uses: actions/cache@v5
        with:
          path: ${{ runner.temp }}/openwrt/dl
          key: openwrt-dl-${{ needs.discover.outputs.branch }}-${{ needs.discover.outputs.commit_sha }}
          restore-keys: |
            openwrt-dl-${{ needs.discover.outputs.branch }}-
            openwrt-dl-

      - name: Link host build/staging caches (self-hosted)
        if: env.IS_GH_RUNNER == 'false'
        run: |
          # staging_dir/host holds built host binaries (go, rust, cmake, python, etc.)
          # build_dir/host holds .built/.configured/.prepared stamps needed to skip rebuild
          # staging_dir/host/ccache is populated by CONFIG_CCACHE=y and caches cross-compiler output
          OPENWRT="${{ runner.temp }}/openwrt"
          mkdir -p "$HOME/.cache/openwrt-staging-host" "$HOME/.cache/openwrt-build-host"
          mkdir -p "$OPENWRT/staging_dir" "$OPENWRT/build_dir"
          ln -sfT "$HOME/.cache/openwrt-staging-host" "$OPENWRT/staging_dir/host"
          ln -sfT "$HOME/.cache/openwrt-build-host" "$OPENWRT/build_dir/host"
          echo "Linked staging_dir/host → $HOME/.cache/openwrt-staging-host ($(du -sh "$HOME/.cache/openwrt-staging-host" 2>/dev/null | cut -f1))"
          echo "Linked build_dir/host → $HOME/.cache/openwrt-build-host ($(du -sh "$HOME/.cache/openwrt-build-host" 2>/dev/null | cut -f1))"

      - name: Cache staging_dir/host (GitHub-hosted)
        if: env.IS_GH_RUNNER == 'true'
        uses: actions/cache@v5
        with:
          path: ${{ runner.temp }}/openwrt/staging_dir/host
          key: openwrt-staging-host-${{ needs.discover.outputs.branch }}-${{ needs.discover.outputs.commit_sha }}
          restore-keys: |
            openwrt-staging-host-${{ needs.discover.outputs.branch }}-
            openwrt-staging-host-

      - name: Cache build_dir/host (GitHub-hosted)
        if: env.IS_GH_RUNNER == 'true'
        uses: actions/cache@v5
        with:
          path: ${{ runner.temp }}/openwrt/build_dir/host
          key: openwrt-build-host-${{ needs.discover.outputs.branch }}-${{ needs.discover.outputs.commit_sha }}
          restore-keys: |
            openwrt-build-host-${{ needs.discover.outputs.branch }}-
            openwrt-build-host-

      - name: Update and install OpenWrt feeds
        working-directory: ${{ runner.temp }}/openwrt
        run: |
          cp feeds.conf.default feeds.conf
          echo "src-git luci_sso https://github.com/Ogglord/luci-sso-feed.git" >> feeds.conf
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          ./scripts/feeds install -a

      - name: Patch OpenWrt source
        working-directory: ${{ runner.temp }}/openwrt
        run: |
          set -euo pipefail
          PATCHES="${GITHUB_WORKSPACE}/patches"

          # patches/ generated against next-r4.8.0.rss.mtk; auto-fix regenerates on upstream change.
          git apply --whitespace=fix "${PATCHES}/001-openssl-packages.patch"
          echo "Applied 001-openssl-packages.patch"

          git apply --whitespace=fix "${PATCHES}/002-feeds-strip-release-dir.patch"
          echo "Applied 002-feeds-strip-release-dir.patch"

          git apply --whitespace=fix "${PATCHES}/003-ib-standalone-apk-fallback.patch"
          echo "Applied 003-ib-standalone-apk-fallback.patch"

          # MHZ19B missing in older commits; pre-populate to avoid syncconfig error in 6.18+.
          grep -q CONFIG_MHZ19B target/linux/generic/config-6.18 || \
            echo "# CONFIG_MHZ19B is not set" >> target/linux/generic/config-6.18

      - name: Apply pesa's config.buildinfo
        working-directory: ${{ runner.temp }}/openwrt
        env:
          RELEASE_DIR: ${{ needs.discover.outputs.release_dir }}
          FORCE_CONFIG_URL: ${{ inputs.force_config_url }}
          BRANCH: ${{ needs.discover.outputs.branch }}
          ASU_VER: ${{ needs.discover.outputs.asu_version }}
          REV_HASH: ${{ needs.discover.outputs.rev_hash }}
          GH_TOKEN: ${{ github.token }}
        run: |
          # Resolved config.buildinfo ensures IB packages match manifest (no missing pkgs at make image).
          # Priority: force_config_url > release_dir (modes 1&2) > Mode 3 fallback.
          if [[ -n "${FORCE_CONFIG_URL:-}" ]]; then
            CONFIG_URL="$FORCE_CONFIG_URL"
            echo "Using explicit force_config_url"
          elif [[ -n "${RELEASE_DIR:-}" ]]; then
            CONFIG_URL="https://raw.githubusercontent.com/pesa1234/MT6000_cust_build/refs/heads/main/${RELEASE_DIR}/targets/mediatek/filogic/config.buildinfo"
          else
            if [[ -z "${RELEASE_DIR:-}" ]]; then
              echo "::error::No release_dir available and no force_config_url set — cannot fetch config.buildinfo"
              exit 1
            fi
            CONFIG_URL="https://raw.githubusercontent.com/pesa1234/MT6000_cust_build/refs/heads/main/${RELEASE_DIR}/targets/mediatek/filogic/config.buildinfo"
          fi
          echo "Fetching config from: ${CONFIG_URL}"
          curl -fsSL "${CONFIG_URL}" -o .config

          # IB_STANDALONE=y copies all built .apks into tarball; patches/ fixes the APK fallback it breaks.
          echo "CONFIG_IB=y" >> .config
          echo "CONFIG_IB_STANDALONE=y" >> .config
          sed -i '/CONFIG_BPF_TOOLCHAIN/d;/CONFIG_USE_LLVM_HOST/d' .config
          echo "CONFIG_BPF_TOOLCHAIN_HOST=y" >> .config
          echo "CONFIG_USE_LLVM_HOST=y" >> .config
          LLVM_BINDIR=$(llvm-config --bindir 2>/dev/null || llvm-config-18 --bindir 2>/dev/null || echo "/usr/bin")
          LLVM_PATH="${LLVM_BINDIR%/bin}"
          echo "Using LLVM_PATH: ${LLVM_PATH}"
          echo "CONFIG_BPF_TOOLCHAIN_HOST_PATH=\"${LLVM_PATH}\"" >> .config

          # ccache wraps target+host gcc; cache dir defaults to staging_dir/host/ccache (persisted above).
          sed -i '/^CONFIG_CCACHE[= ]/d;/^# CONFIG_CCACHE /d' .config
          echo "CONFIG_CCACHE=y" >> .config

          # Disable packages we don't want / can't reliably build.
          # luci-app-nordvpnlite DEPENDS on nordvpnlite, which PKG_BUILD_DEPENDS on rust/host.
          # Disabling only nordvpnlite is insufficient — defconfig re-enables it via the luci-app dep.
          # Use `# CONFIG_X is not set` form (not =n) so defconfig treats it as hard constraint.
          sed -i '/CONFIG_PACKAGE_nordvpnlite[= ]/d;/CONFIG_PACKAGE_luci-app-nordvpnlite[= ]/d;/CONFIG_PACKAGE_onionshare-cli[= ]/d' .config
          echo "# CONFIG_PACKAGE_luci-app-nordvpnlite is not set" >> .config
          echo "# CONFIG_PACKAGE_nordvpnlite is not set" >> .config
          echo "# CONFIG_PACKAGE_onionshare-cli is not set" >> .config

          # Extra packages from extra-packages.txt — compiled into local repo, no upstream drift.
          # Use =m (compile only) not =y (compile+install) to avoid package/install failures
          # when dependencies span feeds not included in the base firmware package index.
          grep -v '^\s*#' "${GITHUB_WORKSPACE}/extra-packages.txt" | grep -v '^\s*$' | \
            while read -r pkg; do echo "CONFIG_PACKAGE_${pkg}=m" >> .config; done

          make defconfig

          # Version pinning after defconfig — getver.sh returns r0-* on depth-1 clone.
          sed -i '/^CONFIG_VERSION_NUMBER=/d;/^CONFIG_VERSION_REPO=/d;/^CONFIG_VERSION_CODE=/d' .config
          echo "CONFIG_VERSION_NUMBER=\"${ASU_VER}\"" >> .config
          # VERSION_REPO: our releases-branch base; pairs with 002-feeds patch for correct feed URLs.
          echo "CONFIG_VERSION_REPO=\"https://raw.githubusercontent.com/${GITHUB_REPOSITORY}/releases/${ASU_VER}\"" >> .config
          if [[ -n "${REV_HASH}" ]]; then
            echo "CONFIG_VERSION_CODE=\"${REV_HASH}\"" >> .config
            # TOPDIR/version overrides getver.sh git path (broken on shallow clone).
            echo "$REV_HASH" > version
            echo "Pinned CONFIG_VERSION_CODE=${REV_HASH} and wrote TOPDIR/version"
          else
            echo "::warning::No REV_HASH available — falling back to getver.sh (r0-*)"
          fi



      - name: Inject APK signing key
        working-directory: ${{ runner.temp }}/openwrt
        env:
          PACKAGE_SIGNING_KEY: ${{ secrets.PACKAGE_SIGNING_KEY }}
          PACKAGE_SIGNING_KEY_PUB: ${{ secrets.PACKAGE_SIGNING_KEY_PUB }}
        run: |
          set -euo pipefail

          printf '%s\n' "${PACKAGE_SIGNING_KEY}" > key-build.rsa
          chmod 600 key-build.rsa
          printf '%s\n' "${PACKAGE_SIGNING_KEY_PUB}" > key-build.rsa.pub
          # Embed public key in rootfs so APK trusts our packages on-device.
          mkdir -p files/etc/apk/keys
          cp key-build.rsa.pub files/etc/apk/keys/pesabuilder-signing.rsa.pub

          echo "Signing key injected, public key embedded in firmware at files/etc/apk/keys/"

      - name: Pre-fetch linux-firmware via git-lfs
        working-directory: ${{ runner.temp }}/openwrt
        run: |
          # pesa/linux-firmware uses git-lfs; clone directly so dl/ gets real content.
          PACKAGE_MK="package/firmware/linux-firmware/Makefile"
          if [ ! -f "$PACKAGE_MK" ]; then
            echo "Package makefile not found, skipping pre-fetch"
            exit 0
          fi

          # Prefer build system resolved values over grepping Makefile.
          SOURCE=$(make --no-print-directory -C package/firmware/linux-firmware val.PKG_SOURCE V=s 2>/dev/null || true)
          VERSION=$(make --no-print-directory -C package/firmware/linux-firmware val.PKG_VERSION V=s 2>/dev/null || true)
          PKG_COMMIT=$(make --no-print-directory -C package/firmware/linux-firmware val.PKG_SOURCE_VERSION V=s 2>/dev/null || true)
          MIRROR_HASH=$(make --no-print-directory -C package/firmware/linux-firmware val.PKG_MIRROR_HASH V=s 2>/dev/null || true)

          # Fall back to grepping if make val.* doesn't work in this fork
          if [ -z "$SOURCE" ]; then
            VERSION=$(grep -E '^PKG_VERSION' "$PACKAGE_MK" | head -1 | cut -d= -f2 | tr -d ' ')
            PKG_COMMIT=$(grep -E '^PKG_SOURCE_VERSION' "$PACKAGE_MK" | head -1 | cut -d= -f2 | tr -d ' ')
            MIRROR_HASH=$(grep -E '^PKG_MIRROR_HASH' "$PACKAGE_MK" | head -1 | cut -d= -f2 | tr -d ' ')
            SOURCE="linux-firmware-${VERSION}.tar.zst"
          fi

          if [[ -z "$VERSION" || -z "$PKG_COMMIT" ]]; then
            echo "Could not parse VERSION or PKG_SOURCE_VERSION — skipping pre-fetch"
            exit 0
          fi

          DL_DIR="${{ runner.temp }}/openwrt/dl"
          mkdir -p "$DL_DIR"
          if [ -f "$DL_DIR/$SOURCE" ]; then
            if [ -n "$MIRROR_HASH" ]; then
              CACHED_HASH=$(sha256sum "$DL_DIR/$SOURCE" | cut -d' ' -f1)
              if [ "$CACHED_HASH" = "$MIRROR_HASH" ]; then
                echo "Cached dl/$SOURCE hash matches PKG_MIRROR_HASH — skipping"
                exit 0
              else
                echo "Cached dl/$SOURCE hash mismatch (cached=$CACHED_HASH, expected=$MIRROR_HASH)"
                echo "Removing stale cached file and re-fetching"
                rm "$DL_DIR/$SOURCE"
              fi
            else
              echo "Already present in dl/: $SOURCE (no MIRROR_HASH to verify)"
              exit 0
            fi
          fi

          echo "Source: $SOURCE"
          echo "Version: $VERSION  Commit: $PKG_COMMIT"
          echo "Mirror hash: ${MIRROR_HASH:-none}"

          echo "Cloning pesa1234/linux-firmware at $PKG_COMMIT with LFS..."
          git clone --filter=blob:none https://github.com/pesa1234/linux-firmware.git "${{ runner.temp }}/linux-firmware-src"
          cd "${{ runner.temp }}/linux-firmware-src"
          git checkout "$PKG_COMMIT"
          git lfs pull
          SUBDIR="linux-firmware-${VERSION}"
          TAR_TIMESTAMP=$(git log -1 --no-show-signature --format='@%ct')
          cd "${{ runner.temp }}"
          mkdir -p "$SUBDIR"
          cp -a linux-firmware-src/. "$SUBDIR/"
          rm -rf "$SUBDIR/.git" "$SUBDIR/.gitmodules"

          TAR_FLAGS="--numeric-owner --owner=0 --group=0 --mode=a-s --sort=name --mtime=$TAR_TIMESTAMP"
          case "$SOURCE" in
            *.tar.xz)  tar $TAR_FLAGS -cJf "$DL_DIR/$SOURCE" "$SUBDIR" ;;
            *.tar.zst) tar $TAR_FLAGS -c "$SUBDIR" | zstd -T0 -19 -c > "$DL_DIR/$SOURCE" ;;
            *.tar.gz)  tar $TAR_FLAGS -czf "$DL_DIR/$SOURCE" "$SUBDIR" ;;
            *.tar.bz2) tar $TAR_FLAGS -cjf "$DL_DIR/$SOURCE" "$SUBDIR" ;;
            *)         echo "Unknown archive format: $SOURCE"; exit 1 ;;
          esac
          rm -rf "$SUBDIR" linux-firmware-src

          ACTUAL_HASH=$(sha256sum "$DL_DIR/$SOURCE" | cut -d' ' -f1)
          echo "Created $SOURCE (sha256: $ACTUAL_HASH)"
          if [ -n "$MIRROR_HASH" ] && [ "$ACTUAL_HASH" != "$MIRROR_HASH" ]; then
            # Patch Makefile so make download accepts our LFS-fetched tarball.
            sed -i "s/^PKG_MIRROR_HASH:=.*/PKG_MIRROR_HASH:=${ACTUAL_HASH}/" \
              "${{ runner.temp }}/openwrt/$PACKAGE_MK"
            echo "Patched PKG_MIRROR_HASH to $ACTUAL_HASH"
          fi

      - name: Run make download
        working-directory: ${{ runner.temp }}/openwrt
        run: |
          make download -j$(nproc) || { echo "Retrying..."; sleep 60; make download -j1 V=s; } # retry with V=s on failure

      - name: Run make world
        working-directory: ${{ runner.temp }}/openwrt
        run: |
          exec > >(tee /tmp/openwrt-build.log) 2>&1 # tee to log for failure healthcheck
          make -j$(nproc) || { echo "Retrying..."; sleep 60; make -j1 V=s; }

      - name: Extract ImageBuilder tarball
        run: |
          TARBALL=$(find ${{ runner.temp }}/openwrt/bin -name "*imagebuilder*.tar.*" | head -1)
          echo "Found: $TARBALL"
          cp "$TARBALL" "$GITHUB_WORKSPACE/"
          ls -lh "$GITHUB_WORKSPACE/"*imagebuilder*.tar.*

      - name: Stage packages and target metadata for publish
        run: |
          set -euo pipefail
          OPENWRT="${{ runner.temp }}/openwrt"
          ARCH="aarch64_cortex-a53"
          TARGET="mediatek/filogic"
          STAGING="${{ runner.temp }}/pkg-staging"

          # Layout matches ASU's releases/{version}/ expectations.
          PKG_BASE=$(find "${OPENWRT}/bin" -type d -path "*packages/${ARCH}" | head -1)
          TARGET_BASE=$(find "${OPENWRT}/bin" -type d -path "*targets/${TARGET}" | head -1)

          echo "Packages base: ${PKG_BASE:-NOT FOUND}"
          echo "Targets base: ${TARGET_BASE:-NOT FOUND}"

          mkdir -p "${STAGING}/packages/${ARCH}" "${STAGING}/targets/${TARGET}/packages"

          if [ -n "${PKG_BASE}" ]; then
            for FEED_DIR in "${PKG_BASE}"/*/; do
              [ -d "${FEED_DIR}" ] || continue
              FEED=$(basename "${FEED_DIR}")
              DEST="${STAGING}/packages/${ARCH}/${FEED}"
              mkdir -p "${DEST}"
              cp "${FEED_DIR}"*.apk "${DEST}/" 2>/dev/null || true
              cp "${FEED_DIR}"packages.adb "${DEST}/" 2>/dev/null || true
              cp "${FEED_DIR}"index.json "${DEST}/" 2>/dev/null || true
            done
          fi

          # feeds.conf: ASU reads feed name from col 2; other cols are placeholders.
          FEEDS_CONF="${STAGING}/packages/${ARCH}/feeds.conf"
          : > "${FEEDS_CONF}"
          for FEED_DIR in "${STAGING}/packages/${ARCH}"/*/; do
            [ -d "${FEED_DIR}" ] || continue
            FEED=$(basename "${FEED_DIR}")
            echo "src/apk ${FEED} ignored" >> "${FEEDS_CONF}"
          done
          echo "Generated feeds.conf:"
          cat "${FEEDS_CONF}"

          # Target kmods under targets/${TARGET}/packages/ — ASU reads them there.
          if [ -n "${TARGET_BASE}" ] && [ -d "${TARGET_BASE}/packages" ]; then
            DEST="${STAGING}/targets/${TARGET}/packages"
            cp "${TARGET_BASE}/packages"/*.apk "${DEST}/" 2>/dev/null || true
            cp "${TARGET_BASE}/packages"/packages.adb "${DEST}/" 2>/dev/null || true
            cp "${TARGET_BASE}/packages"/index.json "${DEST}/" 2>/dev/null || true
          fi

          if [ -n "${TARGET_BASE}" ]; then
            DEST="${STAGING}/targets/${TARGET}"
            for f in profiles.json config.buildinfo feeds.buildinfo version.buildinfo; do
              [ -f "${TARGET_BASE}/${f}" ] && cp "${TARGET_BASE}/${f}" "${DEST}/"
            done
            cp "${TARGET_BASE}"/*.manifest "${DEST}/" 2>/dev/null || true
          fi

          (cd "${STAGING}/packages/${ARCH}" && \
            find . -name '*.apk' -o -name 'packages.adb' -o -name 'index.json' | \
            sort | xargs sha256sum > sha256sums) 2>/dev/null || true
          (cd "${STAGING}/targets/${TARGET}/packages" && \
            find . -name '*.apk' -o -name 'packages.adb' -o -name 'index.json' | \
            sort | xargs sha256sum > sha256sums) 2>/dev/null || true

          PKG_COUNT=$(find "${STAGING}" -name '*.apk' | wc -l)
          echo "Total packages: ${PKG_COUNT}"
          if [ "${PKG_COUNT}" -eq 0 ]; then
            echo "::error::No packages found — check build output structure"
            find "${OPENWRT}/bin" -maxdepth 4 -type d | head -30
            exit 1
          fi

          echo "Staging layout:"
          find "${STAGING}" -maxdepth 4 -type d | sort

          tar -czf "$GITHUB_WORKSPACE/packages-staging.tar.gz" \
            -C "${STAGING}" .

      - name: Generate firmware-packages.txt from pesa's manifest
        env:
          RELEASE_DIR: ${{ needs.discover.outputs.release_dir }}
          BRANCH: ${{ needs.discover.outputs.branch }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          # Manifest guarantees IB DEFAULT_PACKAGES matches built repo — no missing packages at make image.
          if [[ -z "${RELEASE_DIR:-}" ]]; then
            echo "::error::No release_dir — cannot fetch manifest"
            exit 1
          fi
          MANIFEST_NAME=$(gh api \
            "repos/pesa1234/MT6000_cust_build/contents/${RELEASE_DIR}/targets/mediatek/filogic" \
            --jq '.[] | select(.name | endswith(".manifest")) | .name' | head -1)
          if [[ -z "$MANIFEST_NAME" ]]; then
            echo "::error::No .manifest found in pesa's release dir ${RELEASE_DIR}"
            exit 1
          fi
          MANIFEST_URL="https://raw.githubusercontent.com/pesa1234/MT6000_cust_build/refs/heads/main/${RELEASE_DIR}/targets/mediatek/filogic/${MANIFEST_NAME}"
          echo "Fetching manifest: ${MANIFEST_URL}"
          curl -fsSL "${MANIFEST_URL}" | awk '{print $1}' | sort -u > "$GITHUB_WORKSPACE/firmware-packages.txt"
          echo "Packages in manifest: $(wc -l < "$GITHUB_WORKSPACE/firmware-packages.txt")"

      - name: Upload artifact — firmware-packages
        uses: actions/upload-artifact@v5
        with:
          name: firmware-packages
          path: firmware-packages.txt
          retention-days: 14

      - name: Upload artifact — profiles.json
        uses: actions/upload-artifact@v5
        with:
          name: profiles-json
          path: ${{ runner.temp }}/pkg-staging/targets/mediatek/filogic/profiles.json
          retention-days: 14

      - name: Upload artifact — ImageBuilder tarball
        uses: actions/upload-artifact@v5
        with:
          name: imagebuilder-tarball
          path: "*imagebuilder*.tar.*"
          retention-days: 14

      - name: Upload artifact — packages-staging
        uses: actions/upload-artifact@v5
        with:
          name: packages-staging
          path: packages-staging.tar.gz
          retention-days: 14

      - name: Write build-meta.json
        env:
          BRANCH: ${{ needs.discover.outputs.branch }}
          ASU_VERSION: ${{ needs.discover.outputs.asu_version }}
          COMMIT_SHA: ${{ needs.discover.outputs.commit_sha }}
          RELEASE_DIR: ${{ needs.discover.outputs.release_dir }}
        run: |
          # Ship discover outputs so containerize can consume them without re-deriving.
          jq -n \
            --arg branch      "$BRANCH" \
            --arg asu_version "$ASU_VERSION" \
            --arg commit_sha  "$COMMIT_SHA" \
            --arg release_dir "$RELEASE_DIR" \
            '{branch:$branch, asu_version:$asu_version, commit_sha:$commit_sha, release_dir:$release_dir}' \
            > "$GITHUB_WORKSPACE/build-meta.json"
          cat "$GITHUB_WORKSPACE/build-meta.json"

      - name: Upload artifact — build-meta
        uses: actions/upload-artifact@v5
        with:
          name: build-meta
          path: build-meta.json
          retention-days: 14

      - name: Clean up OpenWrt build dir (preserve dl/ for cache)
        run: |
          # Preserve dl/ for cache post-step; rm everything else to free disk.
          if [ -d "${{ runner.temp }}/openwrt/dl" ] && [ ! -L "${{ runner.temp }}/openwrt/dl" ]; then
            mv "${{ runner.temp }}/openwrt/dl" "${{ runner.temp }}/openwrt-dl-cache"
          fi
          rm -rf ${{ runner.temp }}/openwrt
          if [ -d "${{ runner.temp }}/openwrt-dl-cache" ]; then
            mkdir -p ${{ runner.temp }}/openwrt
            mv "${{ runner.temp }}/openwrt-dl-cache" "${{ runner.temp }}/openwrt/dl"
          fi

      - name: Healthcheck — fail (with build log tail)
        if: failure()
        run: |
          LOG_TAIL=$(tail -c 8000 /tmp/openwrt-build.log 2>/dev/null || echo "No build log captured")
          curl -fsS -m 10 --retry 3 \
            --data-binary "Build failed: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }} ${LOG_TAIL}" \
            "https://hc-ping.com/${{ secrets.HEALTHCHECK_BUILD_UUID }}/fail" || true

      - name: Capture build log for Claude
        if: failure() && github.event_name != 'workflow_dispatch'
        run: |
          # Write to workspace file — action with: prompts can't use shell vars.
          tail -c 10000 /tmp/openwrt-build.log > "$GITHUB_WORKSPACE/.failed-build-log.txt" 2>/dev/null \
            || echo "No build log (failure occurred before make world)" \
               > "$GITHUB_WORKSPACE/.failed-build-log.txt"
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin \
            "https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}.git"

      - name: Auto-fix failure with Claude (scheduled builds only)
        if: failure() && github.event_name != 'workflow_dispatch'
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          allowed_tools: "Bash,Read,Edit,Write,Glob,Grep"
          prompt: |
            A scheduled CI build (not manually triggered) just failed for this OpenWrt
            ImageBuilder repo. Read CLAUDE.md first for full pipeline context, then
            diagnose and fix the failure.

            Branch: ${{ needs.discover.outputs.branch }}
            Commit: ${{ needs.discover.outputs.commit_sha }}
            Run:    ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            The build log tail (~10 KB) has been written to .failed-build-log.txt in
            the repo root. Read it to understand the failure.

            Steps:
            1. Read CLAUDE.md for pipeline context, then read .failed-build-log.txt.
            2. Identify the root cause from the log and the relevant workflow/source files.
            3. Apply the minimal fix to the workflow or source files in this repo.
               If the root cause is a changed upstream file in pesa's repo that one of
               our patches no longer applies cleanly to, regenerate that patch file in
               patches/ (fetch the new upstream file, apply the transformation, write
               the new diff) — do NOT revert to inline sed commands.
            4. Delete .failed-build-log.txt — it must not be committed.
            5. Create a fix branch: fix/auto-YYYYMMDD-<2-3-word-slug> (kebab-case).
            6. Commit and push. Then open a PR against main with:
               - Root cause (one sentence)
               - What was changed and why it fixes it
               - The failed run URL

# Containerize runs separately (containerize.yml) — re-runnable without re-doing make world.
